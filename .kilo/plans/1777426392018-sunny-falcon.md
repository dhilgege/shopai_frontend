# Plan: Fix Backend AI Service and Connect Frontend for Product Q&A

## Goal
Fix the backend AI service errors and connect the frontend to properly handle product-related Q&A with suggested prompts.

## Current Understanding
- Frontend has been modified to:
  - Fetch products before sending to AI
  - Send product data and app description to `/ai/chat`
  - Display suggested prompt bubbles
  - Handle loading states properly
- Backend Laravel logs show errors:
  - "Trait 'Laravel\Ai\Concerns\Promptable' not found"
  - "Unmatched '}'" in ChatController.php
- These errors cause blank white screen due to failed AI responses

## Required Changes
### Backend Fixes:
1. Install missing Laravel AI package or create missing Promptable trait
2. Fix syntax error in ChatController.php (unmatched '}' and duplicate class)
3. Ensure `/ai/chat` endpoint properly processes:
   - `message`: user query
   - `products`: array of product data
   - `app_description`: context about ShopAI
4. Configure AI to answer only about products and app
5. Return structured response with `reply` and optionally `quick_replies`

### Frontend (Already Done):
- Product fetching and data transmission
- Suggested prompt bubbles UI
- Loading state handling
- Structured response parsing

## Implementation Steps
### Backend:
1. Check/install required Laravel AI package:
   ```bash
   composer require laravel/ai  # or equivalent
   ```
2. Create/apply missing `Promptable` trait if package doesn't provide it
3. Fix ChatAgent.php duplicate class declaration (lines 23-24)
4. Fix ChatController.php syntax error at line 84
5. Update `/ai/chat` endpoint to:
   - Extract products and app_description from request
   - Construct AI prompt using this data
   - Restrict responses to product/app topics
   - Return JSON with `reply` field
6. Test endpoint with sample request

### Frontend (Verify):
1. Ensure AI repository handles product fetch failures gracefully
2. Confirm remote datasource sends correct data structure
3. Verify bloc parsing handles both old and new response formats
4. Test suggestion bubbles trigger proper messaging

## Backend Requirements for `/ai/chat`:
- Input: `{ message, products[], app_description, user_id }`
- Process: Use products data to answer questions about:
  - Product availability, prices, descriptions
  - Recommendations based on user intent (murah, gaming, terbaru, etc.)
  - App functionality (how to buy, etc.)
- Output: `{ reply: string, quick_replies?: string[] }`
- Fallback: For non-product queries, reply with product/app limitation message

## Potential Issues:
- Missing Laravel AI package causing trait errors
- Syntax errors preventing route loading
- AI service not configured for product context
- Response format mismatch between backend and frontend

## Validation Steps:
1. Backend: `POST /ai/chat` with test data returns 200 and valid JSON
2. Frontend: Send message, see suggested bubbles in empty chat
3. Frontend: Click bubble, send message, get AI response
4. Frontend: Ask product question, get structured helpful answer
5. Frontend: Ask unrelated question, get restriction message
6. Navigation: Chat history persists during loading and back navigation

## Quick Test:
After backend fixes, try asking via frontend:
- "Apa saja produk yang tersedia?"
- "Produk apa yang terbaru?"
- Bagaimana cara membeli?"